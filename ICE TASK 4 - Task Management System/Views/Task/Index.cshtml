@model IEnumerable<ICE_TASK_4___Task_Management_System.Models.TaskModel>

<h2>Task List</h2>

<table class="table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Description</th>
            <th>Deadline</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var task in Model)
        {
            <tr>
                <td>@task.Title</td>
                <td>@task.Description</td>
                <td>@task.Deadline.ToShortDateString()</td>
                <td>
                    <a href="/Task/Edit/@task.Id">Edit</a> |
                    <a href="/Task/Delete/@task.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<a href="/Task/Create">Create New Task</a>
